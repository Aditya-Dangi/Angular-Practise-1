import { CustomAppclassDirective } from './custom-appclass.directive';

describe('CustomAppclassDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomAppclassDirective();
    expect(directive).toBeTruthy();
  });
});
